# Backup Folder

Put only public-safe backup files here.

- Commit `cfms_backup_masked_YYYYMMDD.json` or `cfms_restore_masked_YYYYMMDD.sql`.
- Do not commit `*_FULL_*` files because they can contain student names.
- Keep private/full backups on a local machine or private storage.
