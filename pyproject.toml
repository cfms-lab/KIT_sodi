[project]
name = "kit-sodi-graph-viewer"
version = "0.1.0"
description = "Live Polyscope viewer for the graph embedded in graph.html"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "polyscope>=2.6.1",
    "scipy>=1.14",
]

[tool.uv]
package = false
